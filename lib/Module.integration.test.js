const assert = require('power-assert')
const _ = require('lodash')

const Module = require('./Module')

describe('Module', () => {
  it('Should run as expected', () => {
    const ApplicationModule = {
      name: 'Application'
    }


    /*

    1. Get reducers.
    2. Create store.
    3. Pass store to routes.

    */
  })
})